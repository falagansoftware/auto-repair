package html

templ Button(label, typee, testId string) {
	<button class="bg-sky-800 hover:bg-sky-600 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline" type={ typee } data-testid={ testId }>
		{ label }
	</button>
}
